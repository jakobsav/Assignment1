function[fprime] = getSecondOrderForwardDer(f,h)
% [fprime] = getSecondOrderForwardDer(f)
% Computes fprime(x), the second order approximation for the first derivative
% for the grid function 
% f = [f_1, f_2, ...... ,f_n]
% where f_i = f(x(i))
% and   h   = x(i+1)-x(i)
% fprime is evaluated at points [x(1), ..., x(end-2)]


% Change this line to make it work
fprime = ...
   

end